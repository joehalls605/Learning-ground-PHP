<?php

// Indexed Arrays - rrays that use numeric indices to access their elements.
$colours = array("red", "blue", "green");
echo $colors[1];

// Associative Arrays - Arrays where elements are accessed using named keys rather than numeric indices
$person = array(
"first_name" => "John", 
"last_name" => "Doe"
);

// Multidimensional Arrays -  Arrays containing other arrays, allowing for more complex data structures.
$students = array(
    array("John", "A"),
    array("Jane", "B");
);

?>